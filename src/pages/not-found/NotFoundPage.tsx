import type { FC } from 'react';

import { Typography } from '@/components/UI';

const NotFoundPage: FC = () => {
  return <Typography>This page does not exist at the moment</Typography>;
};

export default NotFoundPage;
